#!/bin/sh

echo "Running pre-commit checks..."

# Run frontend lint
echo "Checking frontend..."
cd frontend && npm run lint 2>/dev/null || echo "Frontend lint check skipped (run npm install in frontend first)"
cd ..

echo "Pre-commit checks passed!"
